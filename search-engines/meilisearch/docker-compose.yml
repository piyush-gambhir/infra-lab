services:
    meilisearch:
        image: getmeili/meilisearch:v1.13.3
        ports:
            - "7700:7700"
        environment:
            - MEILI_NO_ANALYTICS=true
        restart: unless-stopped
