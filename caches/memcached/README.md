# Memcached

This directory contains resources to run Memcached using Docker Compose.

## Usage

1. Copy or create a `docker-compose.yml` file in this directory.
2. Run the following command in this directory:

```powershell
# Start Memcached
docker compose up -d
```

## Configuration

-   Customize the `docker-compose.yml` file as needed for your environment.

## Documentation

-   [Memcached Official Documentation](https://memcached.org/)
